{
  "name": "@openfit/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vue-tsc -b --noEmit && vite build",
    "lint": "vue-tsc -b --noEmit",
    "typecheck": "vue-tsc -b --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@openfit/shared": "workspace:*",
    "@vitejs/plugin-vue": "^5.2.1",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "vite": "^6.0.7",
    "vitest": "^2.1.8",
    "vue-tsc": "^2.2.0"
  }
}
